# Frontend/Workspace env (do not commit real secrets)
#
# Copy to `.env` and fill in values.

# Gemini key used by the frontend build (Vite)
GEMINI_API_KEY=

# Backend base URL (without /api). Frontend will call `${VITE_API_BASE_URL}/api/...`
VITE_API_BASE_URL=http://localhost:4000
